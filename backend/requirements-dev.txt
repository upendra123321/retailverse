# Test-only dependencies (not needed to run the app - see requirements.txt
# for that). Kept separate so the production Docker image doesn't pull in
# test tooling.
-r requirements.txt
pytest==8.3.3
pytest-timeout==2.4.0
httpx==0.27.2
