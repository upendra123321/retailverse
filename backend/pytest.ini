[pytest]
; Prepend backend/ (this file's directory) to sys.path so tests can do
; `from app import db` / `from app.main import app` without a separate
; PYTHONPATH env var or an editable install.
pythonpath = .
testpaths = tests
